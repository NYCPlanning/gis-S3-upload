[PATHS]
root_directory = Path to Bytes Production root directory
log_path = Path to log file
DTM_path = Path to DTM root directory
sde_path = Path to SDE PROD
temp_path = Path to temporary directory

[CREDENTIALS]
region_name = Region name associated with s3 account
endpoint_url = End-point URL associated with s3 account
aws_access_key_id = AWS Access key ID associated with s3 account
aws_secret_access_key = AWS Secret Access key associated with s3 account
